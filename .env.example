# Backend URL for the Next.js rewrite proxy (server-side, resolved at runtime)
BACKEND_URL=http://localhost:8080

# Optional: override the API base URL used by the browser (default: /api/v1 via proxy)
# NEXT_PUBLIC_API_URL=/api/v1
